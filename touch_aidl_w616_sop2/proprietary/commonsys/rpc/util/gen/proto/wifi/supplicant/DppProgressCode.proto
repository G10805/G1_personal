syntax = "proto3";

package wifi.supplicant.dpc;

option optimize_for = LITE_RUNTIME;

enum DppProgressCode
{
    AUTHENTICATION_SUCCESS = 0;
    RESPONSE_PENDING = 1;
    CONFIGURATION_SENT_WAITING_RESPONSE = 2;
    CONFIGURATION_ACCEPTED = 3;
}
