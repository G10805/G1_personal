syntax = "proto3";

package wifi;

import "NanDataPathChannelInfo.proto";
import "NanStatus.proto";

option optimize_for = LITE_RUNTIME;

message NanDataPathConfirmInd
{
    int32 ndpInstanceId = 1;
    bool dataPathSetupSuccess = 2;
    bytes peerNdiMacAddr = 3;
    bytes appInfo = 4;
    NanStatus status = 5;
    repeated NanDataPathChannelInfo channelInfo = 6;
}
