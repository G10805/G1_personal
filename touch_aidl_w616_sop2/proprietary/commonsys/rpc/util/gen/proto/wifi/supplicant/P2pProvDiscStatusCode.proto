syntax = "proto3";

package wifi.supplicant.ppdsc;

option optimize_for = LITE_RUNTIME;

enum P2pProvDiscStatusCode
{
    SUCCESS = 0;
    TIMEOUT = 1;
    REJECTED = 2;
    TIMEOUT_JOIN = 3;
    INFO_UNAVAILABLE = 4;
}
