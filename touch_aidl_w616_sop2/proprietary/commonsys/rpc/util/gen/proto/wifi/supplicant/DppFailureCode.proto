syntax = "proto3";

package wifi.supplicant.dfc;

option optimize_for = LITE_RUNTIME;

enum DppFailureCode
{
    INVALID_URI = 0;
    AUTHENTICATION = 1;
    NOT_COMPATIBLE = 2;
    CONFIGURATION = 3;
    BUSY = 4;
    TIMEOUT = 5;
    FAILURE = 6;
    NOT_SUPPORTED = 7;
    CONFIGURATION_REJECTED = 8;
    CANNOT_FIND_NETWORK = 9;
    ENROLLEE_AUTHENTICATION = 10;
    URI_GENERATION = 11;
}
