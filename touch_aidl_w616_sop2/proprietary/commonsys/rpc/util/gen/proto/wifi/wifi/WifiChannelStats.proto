syntax = "proto3";

package wifi;

import "WifiChannelInfo.proto";

option optimize_for = LITE_RUNTIME;

message WifiChannelStats
{
    WifiChannelInfo channel = 1;
    int32 onTimeInMs = 2;
    int32 ccaBusyTimeInMs = 3;
}
