syntax = "proto3";

package wifi;

import "NanDataPathChannelInfo.proto";

option optimize_for = LITE_RUNTIME;

message NanDataPathScheduleUpdateInd
{
    bytes peerDiscoveryAddress = 1;
    repeated NanDataPathChannelInfo channelInfo = 2;
    repeated int32 ndpInstanceIds = 3;
}
