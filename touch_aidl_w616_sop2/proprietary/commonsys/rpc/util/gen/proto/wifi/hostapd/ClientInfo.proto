syntax = "proto3";

package wifi.hostapd;

option optimize_for = LITE_RUNTIME;

message ClientInfo
{
    string ifaceName = 1;
    string apIfaceInstance = 2;
    bytes clientAddress = 3;
    bool isConnected = 4;
}
