syntax = "proto3";

package wifi.supplicant.madrc;

option optimize_for = LITE_RUNTIME;

enum MboAssocDisallowedReasonCode
{
    RESERVED = 0;
    UNSPECIFIED = 1;
    MAX_NUM_STA_ASSOCIATED = 2;
    AIR_INTERFACE_OVERLOADED = 3;
    AUTH_SERVER_OVERLOADED = 4;
    INSUFFICIENT_RSSI = 5;
}
