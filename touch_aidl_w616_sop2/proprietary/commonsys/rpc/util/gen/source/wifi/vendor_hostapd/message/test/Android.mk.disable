LOCAL_DIR_PATH:= $(call my-dir)

include $(CLEAR_VARS)
LOCAL_PATH := $(LOCAL_DIR_PATH)
LOCAL_MODULE := hostapd-vendor-msg-test
LOCAL_PROPRIETARY_MODULE := true

LOCAL_C_INCLUDES := \
    $(LOCAL_PATH)/../include

LOCAL_SRC_FILES := \
    hostapd_vendor_msg_test.cpp

LOCAL_SHARED_LIBRARIES := \
    libbinder_ndk \
    libcutils \
    liblog \
    libprotobuf-cpp-lite \
    libqti-hostapd-vendor-message \
    libqti_proto_util \
    libqti_rpc_common_util \
    libutils \
    vendor.qti.hardware.wifi.hostapd-V1-ndk

LOCAL_STATIC_LIBRARIES := \
    libqti-hostapd-vendor-proto

include $(BUILD_EXECUTABLE)